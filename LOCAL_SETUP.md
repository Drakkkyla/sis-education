# 🚀 Локальный запуск Кванториум система доп образования

## Быстрый старт

### Вариант 1: Автоматический запуск (рекомендуется)

1. **Запустите MongoDB**
   - Убедитесь, что MongoDB запущен и работает на `localhost:27017`

2. **Запустите скрипт**
   ```batch
   start-local.bat
   ```
   Скрипт автоматически:
   - Проверит MongoDB
   - Проверит и освободит порты при необходимости
   - Установит зависимости (если нужно)
   - Запустит backend и frontend в отдельных окнах

3. **Откройте браузер**
   - Frontend: http://localhost:5173
   - Backend API: http://localhost:5000

---

### Вариант 2: Ручной запуск

#### 1. Запуск Backend

```batch
cd backend
npm install
npm run dev
```

Backend будет доступен на: **http://localhost:5000**

#### 2. Запуск Frontend (в новом терминале)

```batch
cd frontend
npm install
npm run dev
```

Frontend будет доступен на: **http://localhost:5173**

---

## Требования

### Обязательно:
- ✅ **Node.js** (версия 18 или выше)
- ✅ **MongoDB** (запущен локально)
- ✅ **npm** (устанавливается с Node.js)

### Проверка установки:

```batch
node --version
npm --version
mongod --version
```

---

## Настройка

### 1. MongoDB

Убедитесь, что MongoDB запущен:
```batch
# Проверка статуса MongoDB
tasklist | findstr mongod
```

Если MongoDB не запущен, запустите его:
```batch
# Обычно MongoDB запускается как служба Windows
# Или через:
"C:\Program Files\MongoDB\Server\<версия>\bin\mongod.exe"
```

### 2. Файл конфигурации Backend

Создайте файл `backend/.env`:

```env
# MongoDB
MONGODB_URI=mongodb://localhost:27017/kvantorium

# JWT Secret (измените на свой секретный ключ)
JWT_SECRET=your-super-secret-key-change-this-in-production

# Порт (по умолчанию 5000)
PORT=5000
```

### 3. Файл конфигурации Frontend (опционально)

Создайте файл `frontend/.env` (если нужно изменить URL API):

```env
VITE_API_URL=http://localhost:5000/api
```

---

## Полезные команды

### Проверка портов

```batch
# Проверить, занят ли порт 5000
netstat -ano | findstr :5000

# Проверить, занят ли порт 5173
netstat -ano | findstr :5173
```

### Освобождение порта

Если порт занят, используйте скрипт:
```batch
check-port.bat
```

Или вручную:
```batch
# Найти PID процесса
netstat -ano | findstr :5000

# Завершить процесс (замените <PID> на номер процесса)
taskkill /F /PID <PID>
```

### Остановка серверов

- Нажмите `Ctrl+C` в окнах, где запущены серверы
- Или закройте окна командной строки

---

## Решение проблем

### ❌ Порт 5000 уже занят

**Решение:**
```batch
check-port.bat
```
Или завершите процесс вручную (см. выше)

### ❌ MongoDB не запущен

**Решение:**
1. Проверьте, установлен ли MongoDB
2. Запустите MongoDB как службу Windows
3. Или запустите вручную через командную строку

### ❌ Ошибка подключения к MongoDB

**Решение:**
1. Проверьте, что MongoDB запущен: `tasklist | findstr mongod`
2. Проверьте URI в `backend/.env`: `mongodb://localhost:27017/kvantorium`
3. Убедитесь, что MongoDB слушает на порту 27017

### ❌ Модули не установлены

**Решение:**
```batch
# Backend
cd backend
npm install

# Frontend
cd frontend
npm install
```

### ❌ Ошибки компиляции TypeScript

**Решение:**
```batch
# Очистка и переустановка
cd backend
rmdir /s /q node_modules
del package-lock.json
npm install
```

---

## Структура проекта

```
Sis/
├── backend/          # Backend сервер (Node.js + Express + TypeScript)
│   ├── src/
│   │   ├── routes/   # API маршруты
│   │   ├── models/   # Модели MongoDB
│   │   └── server.ts # Точка входа
│   └── .env          # Конфигурация
│
├── frontend/         # Frontend (React + Vite + TypeScript)
│   ├── src/
│   │   ├── pages/    # Страницы
│   │   ├── components/ # Компоненты
│   │   └── services/ # API клиенты
│   └── .env          # Конфигурация (опционально)
│
└── start-local.bat   # Скрипт автоматического запуска
```

---

## Следующие шаги

После успешного запуска:

1. **Создайте первого администратора** (если база данных пустая)
   - Используйте скрипт `backend/src/scripts/seed.ts`
   - Или создайте пользователя через интерфейс регистрации

2. **Заполните базу данных** (опционально)
   ```batch
   cd backend
   npm run seed
   ```

3. **Начните разработку!** 🎉

---

## Полезные ссылки

- Backend API: http://localhost:5000/api
- Frontend: http://localhost:5173
- Health Check: http://localhost:5000/api/health

---

**Приятной разработки!** 🚀

