# 📦 Инструкции по развертыванию проекта

Этот проект можно развернуть двумя способами:

## 🖥️ Локальное развертывание (Windows 11)

Для запуска на локальном компьютере в сети:

👉 **[DEPLOYMENT.md](./DEPLOYMENT.md)** - Полная инструкция по развертыванию на Windows 11

**Быстрый старт:**
1. Запустите `install-server.bat`
2. Запустите `setup-server.bat`
3. Запустите `start-server.bat`

---

## 🌐 Развертывание на хостинге Node.js

Для развертывания на хостинге через SFTP:

### 📚 Документация:

1. **[QUICK_DEPLOY.md](./QUICK_DEPLOY.md)** ⚡ - Быстрая шпаргалка (начните отсюда!)
2. **[DEPLOYMENT_HOSTING.md](./DEPLOYMENT_HOSTING.md)** 📖 - Подробная инструкция
3. **[HOSTING_SETUP.md](./HOSTING_SETUP.md)** 🔧 - Настройка на хостинге
4. **[SFTP_UPLOAD.md](./SFTP_UPLOAD.md)** 📤 - Загрузка файлов через SFTP

### 🚀 Быстрый старт для хостинга:

1. **Соберите проект:**
   ```bash
   # Windows
   build-for-hosting.bat
   
   # Linux/Mac
   bash build-for-hosting.sh
   ```

2. **Загрузите через SFTP:**
   - `backend/` (всю папку)
   - `frontend/dist/` (папку dist)
   - `photo/` (если есть)

3. **Настройте на сервере:**
   - Создайте `backend/.env` (см. примеры в инструкциях)
   - Установите зависимости: `cd backend && npm install --production`
   - Настройте команду запуска: `backend/dist/server.js`

4. **Запустите сервер через панель управления хостинга**

---

## 📋 Что было сделано для развертывания:

✅ Обновлен `backend/src/server.ts` для раздачи статических файлов фронтенда в production  
✅ Созданы скрипты сборки (`build-for-hosting.bat` и `.sh`)  
✅ Созданы примеры конфигурационных файлов  
✅ Настроен CORS для production  
✅ Добавлена поддержка SPA routing (React Router)  

---

## 🔑 Ключевые файлы:

- `backend/.env` - Конфигурация бэкенда (создать на сервере)
- `frontend/.env.production` - Конфигурация фронтенда (если нужен отдельный сервер)
- `backend/dist/server.js` - Главный файл для запуска на хостинге

---

## ❓ Нужна помощь?

1. Проверьте логи сервера в панели управления хостинга
2. Убедитесь, что все файлы загружены правильно
3. Проверьте переменные окружения
4. См. раздел "Решение проблем" в [DEPLOYMENT_HOSTING.md](./DEPLOYMENT_HOSTING.md)

---

## 📝 Структура проекта:

```
Sis/
├── backend/              # Backend сервер (Node.js/Express/TypeScript)
│   ├── src/             # Исходный код
│   ├── dist/            # Скомпилированный код (после сборки)
│   └── uploads/         # Загруженные файлы
├── frontend/            # Frontend (React/Vite/TypeScript)
│   ├── src/             # Исходный код
│   └── dist/            # Собранный фронтенд (после сборки)
├── photo/               # Логотипы курсов
└── [документация]       # Файлы с инструкциями
```

---

**Удачного развертывания! 🎉**

